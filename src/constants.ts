const ModelNames = {
  Patients: 'patients',
};

enum ServiceNames {
  Patients = 'patients',
}

const AppConstants = {
  MONGODB_URL: 'mongoDBUrl',
  MONGO_CLIENT: 'mongoClient',
};

const DBConstants = {
  DB_NAME: 'health-stack',
};

export { AppConstants, DBConstants, ModelNames, ServiceNames };
